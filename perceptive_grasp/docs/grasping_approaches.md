# 抓取方案

perceptive grasp 采用基于立体视觉的单目标三维几何抓取方案。本页说明感知、点云几何、候选筛选和执行之间的关系，以及底盘辅助的介入条件。

![perceptive grasp 系统链路](assets/grasp-system-flow.svg)

## 1. 感知目标

立体相机输出像素对齐的彩色图像和深度信息。yolov8-seg 在彩色图像中检测目标，并输出类别、检测框和分割掩码（mask）。

pipeline 优先采样分割掩码内的有效深度，并从目标周围区域采样桌面点。杯、瓶的目标区域缺少可靠深度时，规划器使用目标轮廓和周围桌面点重建侧抓几何，不会把背景深度直接当作目标表面。

realsense d435i 深度相机直接输出对齐深度。spacemit_las2 双目相机根据双目标定参数和视差推理生成与逻辑左目彩色图对齐的深度。两种后端向上层提供相同的彩色图、深度图和相机内参接口。

## 2. 三维几何

系统使用立体相机内参将采样像素反投影到相机坐标系，再使用 eye-to-hand 标定结果转换到机械臂基座坐标系。几何规划器执行以下处理：

- 使用目标周围点拟合桌面平面。
- 去除桌面附近的目标点和离群深度。
- 计算目标相对桌面的中心、长、宽、高和水平主轴。
- 对杯、瓶根据目标尺寸和夹爪开口生成顶抓与侧抓候选。

该过程使用 opencv 和 c++ 实现，不依赖额外点云框架或抓取模型。两种立体相机后端向规划器提供相同的对齐彩色图、米制深度和反投影接口。

## 3. 候选筛选

普通目标在 `auto` 模式下使用二维分割顶抓链路：根据目标掩码计算短轴抓取像素，从对齐深度计算三维坐标，再从目标上方向下接近。顶抓的像素偏移、tcp 补偿、预抓取高度和下探深度不参与侧抓规划。

杯、瓶在 `auto` 模式下进入三维几何规划。高度和高宽比满足侧抓条件时，系统只生成侧抓候选，避免夹爪落在杯口或物体顶部后抓空。其他类别不会因为三维尺寸自动切换为侧抓。

侧抓使用目标点云和桌面平面生成独立位姿，并按以下顺序执行：

- pipeline 先在当前安全姿态检测目标并锁定侧抓策略，再移动到侧抓观察位。
- 进入侧抓观察位时各关节协调运动，第四关节保持更快的前导进度，使末端尽早离开地面并保持底盘车轮正常承重。
- 到达侧抓观察位后重新检测目标并规划，底盘对齐只能在这次重新检测之后介入。
- 保持夹爪闭合，先转动第一关节靠近目标方向，再校正到目标近侧表面前方的预抓取位。固定爪沿夹爪开合方向偏移目标半宽和机械补偿，避免第一次靠近时扫过目标中心。
- 夹爪张开后，机械臂沿水平方向进入抓取位并闭合夹爪。
- 机械臂先沿桌面法向抬升，再沿接近方向退出，避免夹持目标贴近桌面移动。

预抓取位过于靠近机械臂基座时，pipeline 会在机械臂执行前请求底盘后退。预抓取间隙已满足要求时，侧抓保持当前纵向距离，不套用顶抓的目标中心舒适区；横向偏移仍由底盘对齐。底盘停车后必须重新获取图像、检测目标并规划侧抓路径。

候选按以下条件筛选：

- 目标点数和桌面平面有效。
- 目标夹持宽度不超过配置的夹爪开口。
- 抓取点与桌面保持安全间隙。
- 抓取点位于机械臂工作空间。
- 预抓取位、抓取位和抬升位在规划时限内通过 ik 检查。
- 侧抓完整关节路径中的机械臂碰撞几何与桌面保持安全间隙。

杯、瓶或显式侧抓没有安全候选时，pipeline 在机械臂动作前结束当前规划，不回退到普通目标的二维顶抓链路。

pipeline 对单帧采集、yolov8-seg、三维几何和候选 ik 筛选设置总耗时预算。日志中的 `PERCEPTION_PLANNING` 给出实际耗时和预算；超时结果不会交给机械臂执行。

## 4. 底盘辅助

底盘辅助属于抓取规划闭环，不属于机械臂执行阶段。目标位置超出 `mobile_base.target_x`、`x_tolerance` 和 `y_tolerance` 定义的舒适抓取区时，pipeline 进入 `BASE_ALIGNING`。

底盘每次只执行一个短距离前进、后退或原地转向动作。动作结束后底盘刹车，pipeline 等待画面稳定，再重新获取立体相机帧、检测目标并计算抓取位置。只有重新定位后的目标进入舒适抓取区，状态机才进入机械臂接近阶段。

pipeline 使用最大对齐次数、累计直行距离和视觉停滞门限限制底盘动作。目标位置有可测量的正向改善时继续闭环对齐；没有改善或运动方向错误时，任务进入失败终态，避免持续移动。

## 5. 机械臂执行

执行器使用 pinocchio 求解逆运动学，并按接近、夹取、抬升或退出、放置和归位的顺序控制机械臂。夹爪闭合后，系统结合夹爪位置和负载判断是否抓住目标。检测到空抓或抬升后掉落时，pipeline 返回对应观察位，重新检测并在重试上限内再次抓取；执行器错误进入失败恢复。

## 6. 方案边界

- 适用于桌面或近距离场景中的单目标顶抓和侧抓。
- 依赖准确的立体相机标定、eye-to-hand 标定和目标表面深度。
- 侧抓只在桌面平面、目标高度、夹爪开口、ik 和完整路径均满足约束时启用。
- 不处理遮挡严重、透明、高反光或没有有效视差的目标。
- 底盘只执行局部辅助对齐，不提供导航、避障和全局路径规划。
- 几何筛选只提供桌面间隙和候选可达性保护，不替代完整环境碰撞检测或安全认证。

状态转换见[pipeline 状态机](pipeline_state_machine.md)，参数定义见[抓取配置参考](grasp_config.md)。
